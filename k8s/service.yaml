apiVersion: v1
kind: Service
metadata:
  name: banking-app
  namespace: banking
spec:
  type: NodePort
  selector:
    app: banking-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
